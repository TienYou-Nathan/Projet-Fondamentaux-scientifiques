#include <stdio.h>
#include <stdlib.h>
#define TRUE 1;
#define FALSE 0;
#ifndef ACTIONS
#define ACTIONS
int InOrder(fonctionProc*, int);
int Increasing(fonctionProc*, int ,int);
int Decreasing(fonctionProc*, int ,int);
int average(fonctionProc*);
int size(int);
#endif
